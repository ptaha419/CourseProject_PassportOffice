@model PassportOffice.Models.Application 

@{
    Layout = "_Layout";
    ViewData["Title"] = "Подать заявление";
}

<div class="text-center">
    <h1 class="display-4">Подать заявление</h1> 

@*     <div class="row">
        @using (Html.BeginForm("Create", "Application", FormMethod.Post))
        {
            <div class="form-group">
                @Html.LabelFor(model => model.TypeOfApplicationId)
                @Html.DropDownListFor(model => model.TypeOfApplicationId, Enum
                    .GetValues(typeof(PassportOffice.Models.Application))
                    .Cast<PassportOffice.Models.Application.TypeOfApplicationId>()
                    .Select(x => new SelectListItem { 
                        Text = x.ToString(), Value = ((int)x).ToString() 
                    }), "Выберите тип заявления")
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.StartDate)
                @Html.EditorFor(model => model.StartDate)
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.Description)
                @Html.TextAreaFor(model => model.Description)
            </div>

            <button type="submit">Отправить заявление</button>
        }
    </div> *@
</div>
